# Local Scale Envelope

Status: Partial.

The repo targets local synthetic demos on SQLite. It is not production load infrastructure.

Current safe claim: package boundaries allow SQLite to be swapped later, workflow execution can move out of HTTP handlers, and deterministic traces can become exported observability data.

Local verification should include 100 synthetic work items for queue/evidence/timeline response checks before claiming cockpit-scale readiness.
