# Doctor Evals

README-only M0 placeholder.

This planned package will own deterministic regression and safety evals for Doctor Agent OS. It becomes a real package in M8. Do not add `package.json`, `tsconfig.json`, source files, or build config in M0.

Target checks:

- golden trace diffs
- tool policy assertions
- approval gate assertions
- safety claim checks
- evidence text treated as data, not instruction
